apiVersion: v1
kind: Secret
metadata:
  name: postgres-trip-secret
  namespace: zako-ms
type: Opaque
stringData:
  POSTGRES_DB: trip_db
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: "1234"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-trip
  namespace: zako-ms
spec:
  clusterIP: None
  selector:
    app: postgres-trip
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-trip
  namespace: zako-ms
spec:
  serviceName: postgres-trip
  replicas: 1
  selector:
    matchLabels:
      app: postgres-trip
  template:
    metadata:
      labels:
        app: postgres-trip
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - name: pg
              containerPort: 5432
          envFrom:
            - secretRef:
                name: postgres-trip-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "trip_db"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 15
            periodSeconds: 15
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
